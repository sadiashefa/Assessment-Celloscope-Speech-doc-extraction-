# ============================================================
# Copy this file to .env and fill in values to activate real providers.
# The .env file is gitignored — never commit it.
# Without .env, docker compose up runs entirely on mock adapters.
# ============================================================

# --- Provider selection ---
# mock  → replays fixtures from disk, no credentials needed (default)
# groq  → Groq Whisper API  (get free key at console.groq.com)
# openai → OpenAI Whisper API (fallback if Groq rate-limits)
TRANSCRIPTION_PROVIDER=mock

# mock       → replays fixtures from disk, no credentials needed (default)
# openrouter → OpenRouter vision API (requires OPENROUTER_API_KEY)
DOCUMENT_PROVIDER=mock

# --- Secrets (leave blank in this file, fill in your .env) ---
GROQ_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=

# --- Model (only used when DOCUMENT_PROVIDER=openrouter) ---
OPENROUTER_MODEL=google/gemini-2.0-flash-001
